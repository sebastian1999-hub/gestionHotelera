{% extends "base.njk" %}
{% block titulo %}
    Limpieza nueva
{% endblock %}
{% block contenido %}
    <form action="/limpiezas/{{id}}" method="post" enctype="multipart/form-data">
        
        <label for="fecha">Fecha de limpieza:</label>
        <input type="date" id="fecha" name="fecha" value="{{ Date.now() }}" required>
            {% if errores && errores.observaciones %}
                <p>{{ errores.observaciones.message }}</p>
            {% endif %}
            <label for="observaciones">Descripción de la habitación:</label>
            <textarea id="observaciones" name="observaciones" rows="4"></textarea>
                    <button type="submit">Guardar</button>
                </form>
            {% endblock %}
            <script>
                function obtenerFechaActual() {
                    const hoy = new Date();
                    const año = hoy.getFullYear();
                    const mes = (hoy.getMonth() + 1).toString().padStart(2, '0');
                    const dia = hoy
                        .getDate()
                        .toString()
                        .padStart(2, '0');
                    return `${año}-${mes}-${dia}`;
                }
                document.getElementById('fecha').value = obtenerFechaActual();
            </script>